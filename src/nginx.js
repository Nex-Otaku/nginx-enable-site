const enable = (site) => {
    // TODO
    console.log('Enabled ' + site);
};

const disable = (site) => {
    console.log('Disabled ' + site);
};

module.exports = {
    enable: enable,
    disable: disable
};